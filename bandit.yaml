# Bandit static analysis configuration for Program Discovery Agent (see also B104 fix in app/main.py).
# B311 — random.uniform / random.choice for crawl delays and UA pick (not cryptographic).
# B608 — dynamic SQL fragments built from fixed column allowlists; user input bound via %s.
skips:
  - B311
  - B608
